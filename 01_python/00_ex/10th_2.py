#사용자 정의 함수
#def 예약어 사용 define
#변수명 규칙처럼 사용
#return 값으로 반환하되, 반환값은 여러개로 반환할 수 있음. 튜플 형태로 반환됨

def test1(x, y):
    return x, y, x+y
print(test1(4, 5))

#함수 입력 -> 매개변수 -> Parameter (없을 수 있음)
#처리
#출력 return ->(없을 수 있음)

#종강축하 노래 함수
def sing():
    pass

sing()
